spring:
  datasource:
    url: jdbc:mysql://localhost:3306/evocpp?useSSL=false&serverTimezone=UTC
    username: root
    password: kyawzayar

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080
